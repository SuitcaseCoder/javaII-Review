package shapes;

public class Rectangle {
    // TODO: create a Rectangle subclass of GraphicShape.
    //  Give it the following properties/fields: length, width, and color.
    //  Instantiate Rectangle and assign all of its fields, including
    //  x and y from its parent class.
    //  Call all four methods from the subclass (moveTo, draw, resize,
    //  color).
    //  Implement the abstract methods within the Rectangle subclass
    //  (make the abstract methods do something)
    //
}
